package rules

const (
	msgLowercase = "log message must start with a lowercase letter"
	msgEnglish   = "log message must be in english"
	msgSpecial   = "log message must not contain special symbols or emojis"
	msgSensitive = "log message must not contain sensitive data"
)
